# 천공의 크리스탈 (Sky Crystal Arena) — 작업 기록

**2026-09-17 · 작업 후 전량 원복함**

레퍼런스 컨셉 시트를 받아 맵 비주얼을 새로 만들었다가, **기획 컨셉 대비 퀄리티가
떨어져** 전부 되돌렸습니다. 다시 시도할 때 같은 벽에 다시 부딪히지 않도록
**무엇을 했고, 어디서 막혔고, 왜 막혔는지**를 남깁니다.

- 되돌린 시점: `public/index.html_20260917_천공크리스탈전` · `server.js_20260917_천공크리스탈전`
- 작업본 보관: `public/index.html_20260917_천공크리스탈작업본` · `server.js_20260917_천공크리스탈작업본`
- 레퍼런스: `0_사내공모전 게임화면 관련_260917/ChatGPT Image 2026년 9월 17일 오후 01_10_12.png`

---

## 1. 결론부터 — 왜 격차가 났나

**순수 절차적 생성(이미지 파일 0개)이라는 지금 구조 안에서만 풀려고 했기 때문입니다.**

레퍼런스의 홍채빛 크리스탈, 구름 질감, 천공 성의 실루엣은 **텍스처 없이는 그
수준에 도달할 수 없습니다.** 셰이더 스카이돔 + 절차적 구름/소품으로는
"색과 형태의 배치"까지는 가지만, "재질의 밀도"가 안 나옵니다.

이 제약은 원래 **의도된 것**이었습니다 — 사내망에서 CDN이 막혀도 게임이 뜨도록
외부 의존을 0으로 만든 결과입니다(`CLAUDE.md` 의 '외부 의존 없음'). 비주얼을
레퍼런스 수준으로 올리려면 **이 결정을 부분적으로 되돌려야** 합니다.
자체 서빙이므로 CDN 문제는 없고, 관리할 것은 **용량**뿐입니다.

---

## 2. 실제로 한 작업

| 작업 | 위치 | 결과 |
|---|---|---|
| 테마 3종 교체 | `index.html` `THEMES` · `server.js` `THEME_NAMES` | 캔디/네온/새벽 → 천공의 크리스탈 **한낮 / 여명 / 오로라** (같은 세계의 세 시간대) |
| 소품 생성기 확장 | `buildProps`, `PROP_GEO`, `PROP_MAT` | 테마당 1종 → 여러 종 동시. `island` `ring` `pillar` `spire` `shard` `fall` |
| 폭포가 떨어지는 섬 | `buildProps` 의 `under:` | 먼저 만든 종류의 자리를 기억했다가 그 **밑에** 매닮 |
| 층 팔레트 | `server.js LAYER_DEFS` | 위(시안) → 아래(보라) 6단계 그라데이션 |
| 타일 크리스탈 재질 | `TILE_STYLE` | 거칠기 0.62→0.28, 금속성 0.18→0.30 |
| 모서리 프레넬 발광 | 타일 `onBeforeCompile` | 스치는 각도일수록 밝아짐 — 윗면은 그대로, 실루엣만 |
| **타일 윤곽선** | 타일 프래그먼트 셰이더 | 육각형 거리장(SDF)으로 일정한 폭의 선 |
| 가장자리 크리스탈 기둥 | `buildRimPillars` | 육각 경기장의 **여섯 꼭짓점에만** |
| 층 사이 낙하 연출 | `buildSkyMotes` / `updateSkyMotes` | 작은 결정 150개가 경기장 **바깥** 반경에서 떠돎 |
| 크리스탈 파편 | `spawnDebris` / `updateDebris` | 개별 메시 → **InstancedMesh 풀**, 팔면체 |
| 포인트 발판 색 | `SPECIAL_TYPE_STYLE` | 시안(신속심사) · 연보라(검색) · 연분홍(위상) |

성능은 문제가 아니었습니다 — **드로우콜 54 · 삼각형 155K · 60fps**.
전부 인스턴싱으로 넣었고 예산 안이었습니다.

---

## 3. 핵심 벽 — 옅은 타일 + 옅은 하늘은 명도로 분리할 수 없다

레퍼런스는 **옅은 파스텔 타일 + 밝고 맑은 하늘**입니다. 그런데 이 저장소에는
*"타일이 배경과 구분되지 않는다"* 는 의견을 받고 WCAG로 측정해 고친 이력이 있고,
기준이 **3.0** 입니다.

### 계산 결과

```
6개 층 타일 밝기 (흰색 혼합 0.05 적용)
  L6 0.754 · L5 0.580 · L4 0.501 · L3 0.457 · L2 0.435 · L1 0.439

가장 어두운 타일 밝기                        0.435
→ 6개 층 전부와 대비 3.0 을 얻으려면 하늘 밝기 ≤ 0.112
```

**0.112 는 `#24548c` — 짙은 남색입니다.** "밝고 맑은 푸른 하늘"이 아닙니다.

| 하늘색 | 가장 밝은 타일과의 대비 |
|---|---|
| `#bfe2f7` (레퍼런스에 가까움) | **1.04** ✗ |
| `#7fbde8` | 1.55 ✗ |
| `#4a8fd0` | 2.62 △ |
| `#3d7ec0` (최종 채택) | 3.25 ✓ (가장 밝은 층 기준) / **1.96** (6개 층 최악) |
| `#24548c` | 3.0+ (전 층) — 하지만 남색 |

> **왜 최악값이 더 나쁜가**: 타일이 6가지 밝기로 흩어져 있고 하늘은 하나입니다.
> 하늘을 어느 밝기에 두든 **그중 한 층은 반드시 하늘과 같은 밝기**가 됩니다.
> 층 색이 여섯 개인 한 이 문제는 구조적입니다.

### 그래서 택한 해법 — 명도가 아니라 윤곽

레퍼런스가 실제로 쓰는 방법입니다. 타일이 옅어도 **경계선과 두께**로 형태가
읽힙니다. 프래그먼트 셰이더에 육각형 거리장을 넣어 일정한 폭의 선을 그렸고,
**이건 실제로 잘 동작했습니다** — 화면에서 확인했고 사용자도 "타일 정상적으로
보인다"고 확인했습니다.

```glsl
float ipgHexDist(vec2 p){          // 안쪽 음수 · 경계 0
  const vec3 k = vec3(-0.8660254, 0.5, 0.5773503);
  p = abs(p);
  p -= 2.0 * min(dot(k.xy, p), 0.0) * k.xy;
  p -= vec2(clamp(p.x, -k.z*0.8660254, k.z*0.8660254), 0.8660254);
  return length(p) * sign(p.y);
}
// color_fragment 직후 — 조명 계산 전이라 그림자·반사와 자연스럽게 섞임
float eLine = smoothstep(-0.1800, -0.1300, eD) * vIsCap;
diffuseColor.rgb *= mix(1.0, 0.3000, eLine);
```

**정점 색으로는 불가능합니다.** 타일 윗면이 `CylinderGeometry(…, 6)` 의
삼각팬이라 정점이 7개뿐이고, 정점 색을 칠하면 중앙에서 가장자리로 번지는
**그라데이션**만 나옵니다. 일정한 폭의 선을 그리려면 픽셀마다 경계까지의
거리를 알아야 합니다.

---

## 4. 배운 것 (다음에 그대로 쓸 수 있는 것)

### 이 프로젝트 고유의 제약

1. **인스턴스는 깊이 정렬이 안 됩니다.** 한 `InstancedMesh` 안에서 반투명을
   쓰면 겹친 것들의 순서가 뒤집힙니다. 그래서
   - 타일을 진짜 반투명 크리스탈로 만들 수 없었고(모서리 발광으로 대체),
   - 파편도 투명도 대신 **크기**로 사라지게 했습니다.
   - 붕괴 타일만 진짜 메시로 승격하는 기존 설계가 바로 이 이유입니다.

2. **`mix` 값의 함정.** `TILE_STYLE.mix` 는 층 색을 흰색 쪽으로 당기는 비율입니다.
   종전 층 색이 순색(`#00ffcc` 등)이라 0.62 가 맞았는데, **파스텔 팔레트에
   같은 값을 쓰면 흰색이 됩니다.** 팔레트를 바꿀 때 반드시 같이 봐야 합니다.

3. **소품 배치는 종류마다 방향 정책이 달라야 합니다.** 전부 무작위로 돌리면
   섬이 뒤집히고 기둥이 누워 "떠 있는 하늘"로 안 읽힙니다.

4. **장식물에 충돌체를 지울 필요가 없습니다.** 이 게임의 물리는 `G.tiles`
   하고만 판정하므로, 씬에 올린 장식 메시는 그 자체로 통과됩니다.

### 되살릴 가치가 있는 조각 — 배경과 무관한 성능 개선

> **크리스탈 파편 인스턴싱**
>
> 종전 `spawnDebris` 는 **파편 하나가 메시 하나**였습니다. 색이 타일마다 달라
> 재질을 공유할 수 없어 **파편 하나 = 드로우콜 하나**였고, 상한이 220 이었으니
> 최악의 순간에 드로우콜이 220 늘어납니다. 타일을 546 → 36 으로 줄여 놓은 것이
> 여기서 그대로 되돌아옵니다.
>
> 작업본의 `InstancedMesh` 풀(256, 자유 슬롯 목록) 방식은 **드로우콜 1개**입니다.
> 비주얼과 무관하게 가져올 만합니다.

---

## 5. 다시 한다면

### 방향 A — 텍스처를 도입한다 (레퍼런스 수준을 목표로 할 때)

절차적 생성만으로는 못 갑니다. 최소한으로 넣는다면:

| 넣을 것 | 효과 | 주의 |
|---|---|---|
| **스카이박스 이미지 1장** | 하늘·먼 성·구름을 통째로. 가장 큰 변화 | 테마 3종이면 3장. 용량 관리 |
| **타일 결정 무늬 1~2장** | 홍채빛·결정 패턴. 절차적으로는 한계가 뚜렷 | 타일은 화면의 대부분이라 해상도 대비 효과가 큼 |

자체 서빙이라 CDN 문제는 없고(`CLAUDE.md` '외부 의존 없음' 유지),
관리할 것은 **첫 진입 용량**뿐입니다. 지금 기준선은 3.64MB 이고,
`ASSET_VER` 을 올리는 것을 잊으면 안 됩니다.

### 방향 B — 레퍼런스를 분위기 참고로만 둔다

지금의 절차적 스타일 안에서 완성도를 올립니다. 이때는 **옅은 타일을 포기**하고
층 색에 명도 차를 충분히 주는 편이 낫습니다 — 그래야 가독성 기준(3.0)과
싸우지 않습니다.

### 어느 쪽이든 먼저 정해야 할 것

**층이 6개라는 사실입니다.** 레퍼런스는 5층이고, 이 게임은 특허 절차에 묶인
6층입니다(우선심사 → 실체심사 → 의견제출통지 → 보정 → 청구항 → 아이디어).
층을 줄이면 규칙·라운드 길이·제도 매핑·기획서가 전부 흔들립니다.
레퍼런스의 5단계 무드를 **6단계로 분배**하는 쪽이 맞습니다.

---

## 6. 작업 중 검증이 어려웠던 점 (다음 작업자에게)

브라우저 자동화로 화면을 확인하는 루프가 **여러 번 끊겼습니다.** 연결된
브라우저가 둘이고, 그중 하나는 이 PC 의 `localhost` 에 닿지 않습니다.
비주얼 작업은 화면을 봐야 판단할 수 있으므로, **작업 전에 브라우저 하나를
고정**해 두는 편이 낫습니다.

또 하나 — **백그라운드 탭에서는 렌더가 멈춥니다**(`document.hidden` 이면
`frame()` 이 조기 반환). 캡처가 새까맣게 나오면 버그가 아니라 이것입니다.
페이지 안에서 `stepFrameOnce()` → `renderFrame()` 을 직접 한 번 부르면
현재 상태로 그린 화면을 얻을 수 있습니다.

---

## 7. 원복 시 확인한 것

배경 작업이 같은 날 앞선 게임 작업 **위에** 얹혀 있었기 때문에, 원복 전에
"되돌아가면 안 되는 것"을 먼저 대조했습니다. 아래는 **원복 후에도 살아 있는**
항목입니다.

- 바닥 **두 번 밟기** + 머무름 시계 2.5초
- **연습 모드**(단독 심사) · 관전자 보정 기회
- **어둠 + 선행기술 검색** 재연결 (18초 주기 · 8초 지속 · 44%)
- 맵 **순서 배정**, 맵별 배경음, 효과음(점프 1층/2층 · 클릭), `ASSET_VER 5`
- 모바일 가로 전용 조작
- 버그 수정 3건 (머무름 시계 · 신속심사 사출 · 명단 층 표시)

되돌린 뒤 서버를 띄워 배너와 `/api/config` 로 위 항목이 전부 정상임을
확인했습니다.
