# IP 가디언즈 — 수정 후 전체 검증 프롬프트

수정을 하고 나서 **이 파일 내용을 그대로 붙여 넣으면** Claude Code 가 전 항목을 돌립니다.
행사 전 최종 점검에도 같은 것을 씁니다.

> 이 문서는 "무엇을 재는가"보다 **"어떻게 재면 틀리는가"** 에 지면을 더 씁니다.
> 이 저장소에서 실제로 틀린 값을 믿을 뻔한 일이 여러 번 있었고, 그 값들은 전부
> 오류를 내지 않고 **그럴듯한 숫자**로 나왔습니다.

---

## 붙여 넣을 프롬프트

```
IP 가디언즈 전체 검증을 돌려줘. 아래 순서대로 하고, 각 항목마다 기준값과
비교해서 통과/주의/실패로 판정해줘. 판정이 애매하면 원인을 가를 대조 실험을
추가로 해줘.

[0] 변경 범위 판단
    - 마지막 재점검 커밋과 현재를 정적 대조 (씬에 물체 추가 · 지오메트리 ·
      메시 · 재질 · InstancedMesh · setInterval · rAF · 매 프레임 호출 집합 ·
      스냅샷 필드 · 타일 수 · MAX_PLAYERS · 캐릭터 GLB)
    - 문구·색·규칙 수치·소리만 바뀌었으면 [4][5] 는 건너뛰어도 된다고 알려줘

[1] 규칙(더미) — tools/testbots.js
[2] 서버 부하 A/B — tools/loadbots.js
[3] 장시간 안정성 — tools/soak.js
[4] 클라이언트 성능 — 브라우저 실측 + tools/heapprof.js
[5] 저사양·화질 — ?gpu=1 / ?gpu=2 / Quality.initialLevel()
[6] 네트워크 열화 — tools/netsim.js 를 통과시켜 [1] 재실행
[7] 동시 입장 폭주 — tools/joinburst.js
[8] 브라우저 점검 — tools/browsercheck.js (JS 오류 · 404 · 끊김 복구)
[9] 이동 검증(치팅/정직) — 순간이동·과속·비행·지속하강 + 보고 몰림
[10] HTTP 응답·캐시·압축 헤더

끝나면 한 표로 정리하고, 실패·주의 항목은 원인과 고칠 자리를 짚어줘.
측정값이 기준과 크게 다르면 "코드가 바뀐 것"과 "재는 법이 틀린 것"을
먼저 갈라줘.
```

---

## 항목별 명령과 기준값

포트는 서로 겹치지 않게 씁니다. 기본 개발 서버(3000)는 건드리지 않습니다.

### [0] 변경 범위 판단 — 먼저 이것부터

`tools/perfprobe.js` 머리말이 정한 **다시 재야 하는 변경**:
인원 증가 · 캐릭터 모델 교체 · 씬에 물체 추가 · **매 프레임 도는 코드 추가** ·
스냅샷 필드 추가 · 타일/층 수 변경.
**문구·색·규칙 수치·소리만 바꿨다면 [4][5] 는 생략해도 됩니다.**

```bash
git show <기준커밋>:public/index.html > /tmp/base.html
# .add( / new THREE.*Geometry / Mesh / Material / InstancedMesh /
# setInterval / requestAnimationFrame 개수와
# renderFrame·simulate·stepPhysics 의 '호출 집합' 을 대조
```

기준(2026-09-22): `.add(` 110 · 지오메트리 52 · 메시 82 · 재질 41 ·
InstancedMesh 4 · rAF 4 · `renderFrame`/`simulate`/`stepPhysics` 호출 집합
28/11/13 · 타일 1626 · 스냅샷 필드 `a al cp cy g id l p ry v`(주자당 117바이트).

### [1] 규칙 (더미)

```bash
ROUND_MAX_MS=16000 LOBBY_STALL_MS=10000 PORT=3400 npm start
node tools/testbots.js --port 3400 --bots 4 --die 5,9,13,0 --start --intervene
node tools/testbots.js --port 3400 --bots 3 --die 7,0,0 --start --intervene --drop
node tools/testbots.js --port 3400 --bots 3 --die 5,0,0 --start
node tools/testbots.js --port 3400 --bots 6 --die 0,0,0,0,0,0 --start
```

통과 기준
- 탈락 순위가 죽은 순서와 일치, **생존 시간이 사람마다 다름**(3인 이상)
- `--drop` 에서 관전자 보정 기회 **성공**, 탈락 주자는 **거절**
- 여럿 생존 상태로 시간 만료 → `sudden_death`(서버 로그 `sudden death lv1`)
- **`survived` 는 혼자(연습)일 때만** 나와야 합니다
- 정원 초과 시 새 방이 열림

### [2] 서버 부하 A/B

```bash
node tools/loadbots.js --port 3400 --bots 120 --sec 40
```

기준: **1틱 밀림 0.0~1.1%** · 간격 평균 56~62ms · p95 68~70ms.

> ⚠ **절대값으로 판정하지 마세요.** 그날 PC 사정으로 두 배씩 흔들립니다.
> 반드시 기준 커밋과 A/B 하되 —
> ⚠ **한 번에 한 서버만 띄우세요.** 두 개를 동시에 띄우고 재면 서로 간섭해
>   한 번이 **24.25% · 간격 86.5ms** 로 튑니다(실측).
> ⚠ **순서를 뒤집어 두 번 재세요.** 짝에서 **먼저 잰 쪽이 나빠집니다**(실측으로
>   순서를 바꾸자 우열이 뒤집혔습니다).

### [3] 장시간 안정성 (행사는 몇 시간 돕니다)

```bash
PORT=3400 ROUND_MAX_MS=45000 npm start
node tools/soak.js --port 3400 --min 15 --bots 40 --churn 20
# 옆에서 서버 프로세스 메모리·핸들을 30초마다 기록
```

기준: 메모리가 **평탄**(초반 상승 후 정체) · 핸들 평탄 · **빈 방 0** ·
입·퇴장이 수백 회 누적(방 생성·소멸·승격 경로를 실제로 지남).
실측(2026-09-22, 40봇 9분 · 방 10개 순환 · 입퇴장 224/150회): 메모리 66 → 105 → **126~130MB 평탄**, 핸들 283 고정, 빈 방 0.

### [4] 클라이언트 성능

브라우저(봇으로 방을 채운 뒤 주자로 입장)에서:
- FPS · 프레임 시간 분포 · 드로우콜 · 삼각형 · F2 진단의 화질/보간지연
- 할당량은 **`node tools/heapprof.js <포트> 20`**

기준: FPS **60** · 프레임 p99 **≤ 21ms** · **33ms 넘는 프레임 0개** ·
할당 **주자 0.5KB/프레임 · 관전자 0.15KB/프레임** · 이동 보정 0회.

> ⚠ **`performance.memory` 차이로 할당량을 재면 안 됩니다.** 그 값은 '힙이 얼마나
>   차 있나' 이지 '얼마나 할당했나' 가 아닙니다. 정상 상태에서는 순증가가 0 에
>   수렴합니다(주자 20초에 −0.63MB). 그걸로 재다 **실제의 313배**로 오판했습니다.
> ⚠ 특히 **음수 창을 버리고 양수만 중앙값** 내면 톱니의 봉우리만 골라내는 편향입니다.
> ⚠ **GPU 시간(`perf.shadow`/`perf.groups`)은 vsync 에 걸리면 못 잽니다** —
>   절대 시간이 16~20ms 에 붙으면 그 표로는 A/B 가 안 됩니다.
>   `perf.sanity()` 를 먼저 통과시키고, 필요하면
>   `chrome --disable-gpu-vsync --disable-frame-rate-limit` 로 띄우세요.
> ⚠ **관전자로 들어가면 물리가 안 도는데도 그럴듯한 값이 나옵니다.** 역할을 먼저 확인.

### [5] 저사양·화질

```
?gpu=1 → 등급1 · 시작 화질 2 보통 · 픽셀비 1 · AA 꺼짐 · 그림자맵 켜짐 · PUFF 2
?gpu=2 → 등급2 · 시작 화질 4 최소 · 픽셀비 0.65 · 프레임스킵 1 · 그림자맵 꺼짐 · PUFF 1
5단계 픽셀비 2 / 1.25 / 1 / 0.8 / 0.65
```

> ⚠ **시작 화질을 `Quality.level` 로 확인하면 안 됩니다** — 빠른 PC 에서는 몇 초 만에
>   적응형이 올려 버립니다(등급1 에서 2 → 0). **`Quality.initialLevel()`** 을 직접 부르세요.
> ⚠ 끝나면 `localStorage.removeItem('ipg.quality')` — 안 지우면 다음 접속 화질이 달라집니다.

### [6] 네트워크 열화 (행사장 WiFi)

```bash
node tools/netsim.js --in 3500 --to 3400 --delay 80  --jitter 40
node tools/netsim.js --in 3500 --to 3400 --delay 200 --jitter 80
# 그 뒤 [1] 과 [7] 을 포트 3500 으로 다시
```

기준: 왕복 **200±80ms 까지 서버 이동 보정 0회** · 규칙 정상.

> ⚠ 반드시 **지연 0 으로 같은 프록시를 통과시키는 귀무 대조**를 먼저 하세요.
>   중계 자체가 깨져 있으면 전부 게임 탓으로 보입니다(실제로 그랬습니다).

### [7] 동시 입장 폭주

```bash
node tools/joinburst.js --port 3500 --people 24 --wait 22
node tools/joinburst.js --port 3500 --people 40 --wait 25
```

기준: **전원 입장**(재연결을 거치는 사람이 있어도 됨) · 입장 p50 0.2~0.9초 ·
최대 5초 이내 · 방이 정원까지 고르게 참.
실측(2026-09-22): 24명/40명 모두 100% · 200±80ms 에서 최대 4.8초.

> ⚠ 연속 실행하지 마세요 — 앞 회차 소켓이 남아 **실패로 보입니다**(실측 19/24).
>   각 조건 사이에 서버를 새로 띄우고 몇 초 쉬세요.
> ⚠ '인원 분포' 가 정원을 넘어 보여도 서버 버그가 아닙니다 — 재연결로 자리가
>   비었다 다시 찬 것을 합산한 것입니다. 실제 동시 인원은 `GET /api/rooms` 로 보세요.

### [8] 브라우저 점검

```bash
node tools/browsercheck.js 3400
```

기준: 라운드 중 **자바스크립트 오류 0건** · 화면 살아 있음 ·
**TCP 6초 끊김 → 5초 안에 재연결** · 복구 후 화면 정상.
404 는 선택 에셋(소리 등)만 허용.
실측(2026-09-22, 아이콘 추가 뒤): 오류 0 · 재연결 0.3~3.6초 · **아이콘 404 0건**.
남는 404(`road_texture.png`·`bgm_lobby.mp3`·효과음)는 **선택 에셋**이라 정상입니다 —
소리가 열린 회차에만 나고, 없으면 폴백합니다.

### [9] 이동 검증

```bash
PORT=3700 ROUND_MAX_MS=600000 LOBBY_STALL_MS=8000 npm start
node tools/movecheck.js 3700          # --quick 으로 짧게
```

정직한 주자(수평·낙하 × 보고 몰림 50/150/300/500/800ms) 10경우와
치팅 6종(순간이동·과속·비행·등반·지속하강·하강 순간이동)을 한 번에 봅니다.

기준: 정직 **전부 0회** / 치팅 **전부 차단**.

추가로 **층 전환 한 프레임 끌림**을 전수로 보세요(층쌍 × 회전각 0~60° × 방향 × 반지름).
기준: **서버 MAX_STEP(6.0) 미만**. 2026-09-22 실측 0. 6.26 을 넘으면 보정이 32회씩 연쇄합니다.
실측(2026-09-22): 10/10 통과 · 치팅 6/6 차단.

> ⚠ 낙하 시험에서 **바다에 닿았다고 위로 되돌리면 안 됩니다** — 그 복귀가 100유닛
>   상승이라 서버가 막는 것이 정상인데, 표에는 '정직한 낙하가 12번 보정당함' 으로
>   찍혀 **없는 버그를 만듭니다**(실제로 그렇게 나왔습니다).

### [10] HTTP 응답·캐시·압축

```bash
curl -sI -H "Accept-Encoding: gzip, br" http://localhost:3400/<경로>
```

기준: HTML `no-cache` · `?v=` 붙은 것 `max-age=31536000, immutable` ·
**glb·mp3·png 는 압축하지 않음**(`content-encoding` 없음) · 텍스트는 br/gzip.

아이콘·매니페스트도 함께 보세요 — `/favicon.ico` `/apple-touch-icon.png`
`/icon-192.png` `/icon-512.png` `/manifest.json` 전부 **200** 이어야 합니다.
매니페스트는 크롬 자체 파서로 확인하는 것이 확실합니다(CDP `Page.getAppManifest`) —
2026-09-22 실측 파싱 오류 0건 · `display: fullscreen` · `orientation: landscape`.

---

## 판정표 양식

| 항목 | 기준 | 이번 | 판정 |
|---|---|---|---|
| [1] 규칙 | 전부 통과 | | |
| [2] 부하 | 밀림 ≤1.1% | | |
| [3] 장시간 | 메모리 평탄·빈 방 0 | | |
| [4] 클라이언트 | 60fps·p99≤21ms·0.5KB/프레임 | | |
| [5] 저사양 | 문서와 일치 | | |
| [6] 열화망 | 200ms 에서 보정 0 | | |
| [7] 동시입장 | 전원 입장 | | |
| [8] 브라우저 | 오류 0·재연결 | | |
| [9] 이동검증 | 정직 0 / 치팅 차단 | | |
| [10] 헤더 | 문서와 일치 | | |

---

## 이 저장소에서 실제로 틀렸던 측정 (다시 밟지 마세요)

| 증상 | 진짜 원인 |
|---|---|
| 그림자 절감이 부호까지 뒤집힘 | **vsync** 에 걸려 값이 16~20ms 에 붙음 |
| 같은 코드인데 뒤에 잰 쪽이 141% 느림 | 짝 사이 이벤트 루프 양보 → 위치 편향 |
| 프레임당 130KB 할당 | `performance.memory` 로 쟀고, **양수 창만 중앙값** |
| 힙 샘플러가 18MB 를 0.06MB 로 보고 | 형식배열은 **V8 힙 밖** — 대조를 일반 배열로 |
| 부하 24% 폭증 | 두 서버를 **동시에** 띄우고 잼 |
| 시작 화질이 문서와 다름 | 적응형이 이미 올림 — `Quality.initialLevel()` 로 |
| 회전층 난간이 안 막힘(고친 뒤에도) | `stepFrameOnce()` 가 **회전각을 매 프레임 덮어씀** |
| 층 분기를 아예 안 탐 | 층 **번호**와 배열 **인덱스** 혼동(`G.layerY` 는 번호) |
| 동시 입장 19/24 실패 | 앞 회차 소켓 잔류 + 도구가 `reconnection:false` |
| 접속 전원 실패(xhr poll error) | netsim 이 **청크 순서를 뒤섞음**(TCP 깨짐) |
| 측정값이 전부 0 인데 '이상 없음' | **관전자로 입장** — 물리가 안 돎 |
| 배치만으로 보정 144회 | **순간이동 배치**가 서버에 걸림 — `dev_teleport` 면제를 쓰세요 |
| 회전층 버그가 '안 남' | 자리 고른 뒤 2~3초 지나 **각도가 19° 돌아** 최악 조합을 비껴감 |
| 고쳤는데 옛 결과가 나옴 | A/B 하느라 되돌려 둔 **옛 파일 위에 다음 수정**을 얹음 |

**공통 교훈: 모든 측정 도구에 귀무 대조를 먼저 돌리세요.**
